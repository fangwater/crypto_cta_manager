# BEGIN managed: crypto cta manager
# The jp-meta 4191 site conf is regenerated whole from nginx_locations.txt
# by setup_nginx_4191.sh. Keep exactly one external: row here: the snippet
# owns every /manager, /cta-api and /exec_tradeNN location (SPA try_files,
# immutable assets, auth_request gates). Plain proxy rows for those paths
# would duplicate the snippet's locations and drop the auth gates; the
# /manager/api/ row also defeats the generator's /manager guard.
# install_jp_meta_nginx.sh upserts this block and installs the snippet.
/manager/ external:/etc/nginx/snippets/crypto_cta_manager.conf
# END managed: crypto cta manager
